import { Placeholder } from '../components/Placeholder';

export function TeamPage() {
  return (
    <Placeholder title="Team" emoji="👥">
      <p className="muted">
        Delegation tracker for ~10 reports: per-person tickets with status, due and follow-up
        dates, recurring flag, and a daily follow-up nudge. See <code>mockups/team.html</code>.
      </p>
    </Placeholder>
  );
}
